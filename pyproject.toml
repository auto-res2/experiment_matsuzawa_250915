[project]
name = "phoenix_relax_experiments"
version = "0.1.1"
description = "Re-implementation of PHOENIX-RELAX experimental suite"
authors = [{name = "ML Researcher", email = "research@example.com"}]
readme = "README.md"
requires-python = ">=3.9"

# -----------------------------------------------------------------------------
# Dependencies – kept minimal so that the smoke-test remains light.
# -----------------------------------------------------------------------------

dependencies = [
  "torch>=1.13.0",
  "diffusers>=0.24.0",
  "transformers>=4.37.0",
  "tqdm>=4.64.0",
  "pyyaml>=6.0",
  "numpy>=1.23.0",
  "pillow>=10.0.0",
  "torchvision>=0.14.0",
]

[project.urls]
Homepage = "https://example.com/phoenix-relax"
