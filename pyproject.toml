[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cajun_gnn_experiment"
version = "0.1.0"
dependencies = [
  "numpy>=1.21.0",
  "torch>=1.9.0",
  "PyYAML>=6.0",
  "argparse",
]
