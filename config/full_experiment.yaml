# Full-scale configuration – still small enough to run quickly in CI,
# but larger than the smoke test so we get visibly different metrics.
experiment_name: "garden_full_experiment"

training:
  epochs: 10
  batch_size: 64

dataset:
  name: "synthetic"
  n_samples: 5000
  n_features: 20

