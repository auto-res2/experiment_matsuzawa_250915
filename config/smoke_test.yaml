# Lightweight configuration for CI / quick validation
experiment_name: "garden_smoke_test"

training:
  epochs: 2   # keep tiny for fast CI
  batch_size: 16

dataset:
  name: "synthetic"
  n_samples: 200
  n_features: 20
