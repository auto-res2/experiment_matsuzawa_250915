"""Main execution script.
Because the original Experiment Code section was empty, no runnable workflow
can be reconstructed.  We raise an explicit error so that CI fails loudly
rather than proceeding with undefined behaviour.
"""
import sys

raise RuntimeError("Experiment code missing. Cannot run experiment workflow.")
